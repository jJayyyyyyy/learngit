#include <stdio.h>


/*只有根据之前玩51的经历,大致得到一些想法
 *
int checkKeyPressIn1Second(){
    int count=0;
    //initTimer();
    while(1){
        //key == 0表示键盘按下
        if(key == 0){
            delay(10);
            if(key == 0){
                count++;
                //TODO: 开启定时器
            }
        }
        if(到1秒){
            if(count > 1){
                return 1;
            }
            //TODO: 重置计数器
        }
    }
}
*/

int main()//主函数
{
    return 0;
}
